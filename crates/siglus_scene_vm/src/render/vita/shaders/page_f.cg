// A page-wipe fragment (render/shaders/page_wipe.wgsl).
float4 main(float2 uv : TEXCOORD0, uniform sampler2D page_tex : TEXUNIT0) : COLOR
{
    float4 color = tex2D(page_tex, uv);
    if (color.a <= 0.0) {
        discard;
    }
    return color;
}
